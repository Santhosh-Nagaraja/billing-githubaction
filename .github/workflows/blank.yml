# This is a basic workflow to help you get started with Actions
 
name: Billing-App
 
# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "master" branch
  pull_request:
    types: [closed]
    branches:
      - release
 
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
env:
    image_version: ${{ github.run_number }}
 
jobs:
  terraform:
    if:
      github.event.pull_request.merged == true
    runs-on: ubuntu-latest
 
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
 
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: 1.0.5
 
      - name: Terraform Init
        run: terraform init
 
      - name: Terraform Validation
        run: terraform validate
 
      - name: Terraform Plan -var image_tag=${image_version}
        run: terraform plan        
 
      - name: Terraform Apply
        run: terraform apply -auto-approve -var image_tag=${image_version}
      
  send-job:
    if: github.event_name == 'push' && needs.first-job.outputs.skip == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run second job steps
        run: |
          echo "Running steps in the second job..."
          # Add your steps here
